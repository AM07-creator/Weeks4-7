using UnityEngine;

public class TimedZam : MonoBehaviour
{
    //This script will be used to make a timer cause the "Zam!" gameObject respawn. Using the timer from its code, getComponent will be placed on the CubeDude Respawn Button to make its effects trigger automatically after a few seconds

    

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
